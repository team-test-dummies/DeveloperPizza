package com.revature;

public class Main {

    // creates a javalin instance, connects to database, listens on port
    public static void main(String[] args) {
        // create a javalin application
        // map to static files directory 'public'
        /* static files are consulted if no endpoint matches a users query */
        /* a path that ends on a directory will default to an index.html file*/
        // connect to JDBC
        /* make sure you environment variables are set accordingly */
        // start javalin application and listen on port
    }
}
